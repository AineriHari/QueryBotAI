GOOGLE_API_KEY="your-google-api-key"
MODEL_NAME="your-model-name"
SERVER_NAME="your-server-ip-or-localhost" # Enter the IP address of the machine running localLLM if needed; otherwise, it defaults to localhost.
SERVER_PORT="your-server-port" # Enter the PORT for localLLM if needed; otherwise, it defaults to 7860.
